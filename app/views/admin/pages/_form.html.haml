= form_for([:admin, @page]) do |f|
  = render 'shared/error_messages', target: @page
  .field
    = f.label :name
    %br/
    = f.text_field :name
  .field
    = f.label :content
    %br/
    = f.text_area :content
  .actions
    = f.submit