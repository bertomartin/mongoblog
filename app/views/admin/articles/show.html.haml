/ %h3= @article.title
/ %ul.list-inline
/   %li.list-inline-item=link_to 'Back to List', admin_articles_path
/   %li.list-inline-item &nbsp;|&nbsp;
/   %li.list-inline-item=link_to 'Edit Article', edit_admin_article_path(@article)
/ %hr
/ .content=simple_format @article.content

/ %p=link_to "See comments", render action: :show

/ %h2 Comments

/ %ul.list-unstyled
/   -@article.comments.each do |comment|
/     %li
/       %blockquote
/         %p=comment.body
/         %p=comment.name
/ %br/
.row
  .col-md-8.col-md-offset-2
    %h3= @article.title

    <span>
    %em="By "      
    - length = @article.authors.length
    - names = "" 
    - if length == 1
      %em=@article.authors[0]
      %em=@article['created_at']
    - if length == 2
      %em=@article.authors[0] + " & " + @article.authors[1] 
      %em=@article['created_at']
    -if length > 2
      -art=""
      -for i in 1..length-2
        -art = art + ", " + @article.authors[i]
        -names=@article.authors[0] + art + " & " + @article.authors[length-1]
      %em=names

      %em=@article['created_at']
    </span>
    .content=@article.content

    %hr

    %h2 Comments

    %ul.list-unstyled
      -@article.comments.each do |comment|
        %li
          %blockquote.blockquote
            %p.m-b-0=comment.body
            %footer.blockquote-footer
              =comment.name
              /# %em=time_ago_in_words comment.id.generation_time
            -if comment. body
              =link_to 'delete', admin_article_comment_path(comment.article.id, comment.id), controller: 'comments', method: :delete, data: {confirm: 'Are you sure?' } 
    %br/












